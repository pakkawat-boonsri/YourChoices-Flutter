enum EatHereOrTakeHome {
  eatHere,
  takeHome,
}

enum RadioTypes {
  nochange,
  priceIncrease,
  priceDecrease,
}

enum OrderTypes {
  accept,
  collectToHistory,
  
  pending,
  processing,
  completed,
  failure,
}
